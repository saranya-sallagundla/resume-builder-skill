## What changed

## Why

## Checklist
- [ ] `make test` passes locally
- [ ] If a reference file changed, `make sync-docs` was run
- [ ] Sample resume score did not drop (CI regression gate)
- [ ] No fabricated content added to examples — every sample number is illustrative and labelled as such
